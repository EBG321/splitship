specVersion: "v1beta"
serviceId: "splitship-1"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "splitship-1-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
